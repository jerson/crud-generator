imports:
- { resource: build.yml }
- { resource: services.yml }
- { resource: parameters.yml }

parameters:

  application:
    name:         CrudGenerator
    slug:         crud-generator
    description:  "Genera Aplicaciones simples"
